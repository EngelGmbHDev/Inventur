{
  "name": "inventur",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server/index.js",
    "setpin": "node server/setpin.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute inventur --remote --file=schema.sql"
  }
}
